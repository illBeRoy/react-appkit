/**
 * @module tray
 * APIs to create and use application tray icons.
 * Application tray icons are displayed in the system tray (macOS/Linux) or in the notification area (Windows).
 * This module exports the components to create tray icons and menus, under the "src/tray.tsx" file.
 */

import {
  Tray,
  TrayMenu,
  TrayMenuItem,
} from '@react-appkit/runtime/main/tray/components';

export { Tray, TrayMenu, TrayMenuItem };
