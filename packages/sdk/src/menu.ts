/**
 * @module menu
 * APIs to create and use application menus.
 * Application menus are displayed as the menu bar at the top of the screen (macOS) or in each individual window (Windows/Linux).
 * This module exports the components to create menus, under the "src/menu.tsx" file.
 */

import {
  ApplicationMenu,
  Menu,
  MenuItem,
} from '@react-appkit/runtime/main/menu/applicationMenu/components';

export { ApplicationMenu, Menu, MenuItem };
